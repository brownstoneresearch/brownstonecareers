{
  "name": "brownstone-careers-cloudflare",
  "version": "4.0.0",
  "private": true,
  "description": "Brownstone Careers website deployed as a Cloudflare Worker with static assets and Resend email delivery.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
